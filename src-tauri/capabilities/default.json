{
  "identifier": "default",
  "description": "Default capability granting access to the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
